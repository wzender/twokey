services:
  - type: web
    name: twokey-levantine-coach
    env: python
    plan: free
    region: oregon
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:server --host 0.0.0.0 --port $PORT
